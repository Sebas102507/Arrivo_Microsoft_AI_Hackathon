"""Arrivo on the Microsoft Foundry Agent Service.

This package creates and runs a persistent Foundry agent whose knowledge is the
`arrivo-kb` Azure AI Search index (the Foundry IQ grounding layer). It is the
hosted-agent counterpart to the local reasoning pipeline in ``backend/agent.py``.
"""
